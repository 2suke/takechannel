%div
  - if user_signed_in?
    %ul#member-menu.menu
      %li
        = link_to('home', root_path)
      %li
        = link_to('スレッド立てる', new_board_path)
      %li
        = link_to('ログアウト', destroy_user_session_path)
  - else
    %ul#common-menu.menu
      %li
        = link_to('home', root_path)
      %li
        = link_to('ログイン', new_user_session_path)
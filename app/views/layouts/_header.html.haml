%div
  - if user_signed_in?
    %ul#member-menu.menu
      %li
        = link_to('ログアウト', destroy_user_session_path)
  - else
    %ul#common-menu.menu
      %li
        = link_to('ログイン', new_user_session_path)
      %li
        = link_to('スレッド立てる', create_board_path)